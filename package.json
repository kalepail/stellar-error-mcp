{
  "name": "stellar-error-mcp",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "provision:ai-search": "node scripts/provision-ai-search.mjs",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:live-rpc": "vitest run tests/live-rpc.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "@stellar/stellar-sdk": "^15.0.1",
    "@stellar/stellar-xdr-json": "^23.0.0",
    "@toon-format/toon": "^2.1.0",
    "agents": "^0.8.7",
    "lossless-json": "^4.3.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.79.0"
  }
}
